import React from "react";

export default function MenuItem(props) {

    return (
        <form>
            <input type="tex" placeholder="quantity"/>
            <button>Submit</button>
        </form>
    );
}
